import { styled } from "@/styles";

export const Content = styled('div', {
  width: '100%',
  maxWidth: '48rem',
  margin: '0 auto',

  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center'
})